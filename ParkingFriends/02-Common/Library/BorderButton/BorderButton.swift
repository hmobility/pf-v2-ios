//
//  BorderButton.swift
//  ParkingFriends
//
//  Created by PlankFish on 2020/01/03.
//  Copyright © 2020 Hancom Mobility. All rights reserved.
//

import UIKit

class BorderButton: UIButton {
    /*
    @IBInspectable var normalBorderColor: UIColor?
    @IBInspectable var disabledBorderColor: UIColor?
    @IBInspectable var highlightedBorderColor: UIColor?
    @IBInspectable var selectedBorderColor: UIColor?
    
    // MARK: Initialize

    override init(frame: CGRect) {
        super.init(frame: frame)
    }

    required init?(coder aDecoder: NSCoder) {
        super.init(coder: aDecoder)
    }
    
    // MARK: - Property Variables

    override var borderColor: UIColor? {
        didSet {
            self.normalBorderColor = self.borderColor
            self.highlightedBorderColor = self.borderColor?.withAlphaComponent(0.4)
        }
    }
    
    override var isHighlighted: Bool {
        didSet {
            if self.borderWidth > 0 {
                self.layer.borderColor = self.isHighlighted ? self.highlightedBorderColor?.cgColor : self.normalBorderColor?.cgColor
            }
        }
    }
    
    override var isSelected: Bool {
        didSet {
            if self.borderWidth > 0 {
                self.layer.borderColor = self.isSelected ? self.selectedBorderColor?.cgColor : self.normalBorderColor?.cgColor
            }
        }
    }
    
    override var isEnabled: Bool {
        didSet {
            if self.borderWidth > 0 {
                self.layer.borderColor = self.isEnabled ? self.normalBorderColor?.cgColor : self.disabledBorderColor?.cgColor
            }
        }
    }
    */
    // MARK: - Layout
    
    /*
    // Only override draw() if you perform custom drawing.
    // An empty implementation adversely affects performance during animation.
    override func draw(_ rect: CGRect) {
        // Drawing code
    }
    */

}
